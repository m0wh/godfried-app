interface GodfriedConfig {
  maxLength?: number
  tempo?: number
  swing?: number
}

interface TrackConfig {
  id: number
}

interface RythmParams {
  beats?: number
  offset?: number
  length?: number
}
